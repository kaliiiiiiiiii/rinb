{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Config",
  "type": "object",
  "properties": {
    "arch": {
      "$ref": "#/$defs/Arch",
      "default": "x64"
    },
    "editon": {
      "type": "string",
      "default": "Professional"
    },
    "lang": {
      "description": "Lang code, e.g. en-US",
      "type": "string",
      "default": "en-US"
    }
  },
  "$defs": {
    "Arch": {
      "type": "string",
      "enum": [
        "x64",
        "arm64"
      ]
    }
  }
}